# Rewards Module

Full module rules live in `AGENTS.md` in this same folder — read that file before making any change here. It is not auto-loaded by Claude Code; this file exists only so the pointer itself loads automatically.
